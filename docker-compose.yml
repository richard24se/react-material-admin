version: '3'

services:
  react:
    build: 
      context: .
      dockerfile: ./docker/react.Dockerfile
    command: >    
      sh -c "ls -ls && yarn start" 
    #volumes:
    #  - ./react:/react
    ports:
      - "3000:3000"
